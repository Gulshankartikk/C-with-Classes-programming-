#include<iostream>
using namespace std;
int main()
{
    int number = -50;
   if(number > 0)
   {
     cout<<"it is positive number";
   }
   else if(number<0)
   {
     cout<<"it is negative number";
   }
   else 
   {
     cout<<"it is zero";
   }
}